export interface CallToActionData {
    phoneNumber: string;   // Emergency phone number
    description: string;  // Brief description or call-to-action message
    contactLink: string;  // URL for contacting (e.g., a form or chat link)
    learnMoreLink: string; // URL for more information
}